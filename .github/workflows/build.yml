name: Build

on:
  workflow_dispatch:

jobs:
  build:
    name: Minimal workflow to build default external
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: 'recursive'
        fetch-depth: '0'

    - name: build_default
      run: make
