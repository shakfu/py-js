#include "../common.xcconfig"

BUILD = $(HOME)/.build_pyjs/lib
PREFIX = $(BUILD)/Python.framework/Versions/${VERSION}
PY_HEADERS = $(PREFIX)/include/python$(VERSION)$(ABIFLAGS)
PY_LIBS = $(PREFIX)/lib
PY_LDFLAGS = -lpython$(VERSION)$(ABIFLAGS) -ldl

