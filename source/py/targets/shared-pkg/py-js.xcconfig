#include "../common.xcconfig"

VERSION = 3.9
SUFFIX =
PREFIX = $(SUPPORT)/python$(VERSION)
PY_HEADERS = $(PREFIX)/include/python$(VERSION)$(SUFFIX)
PY_LIBS = $(PREFIX)/lib
PY_LDFLAGS = -lpython$(VERSION)$(SUFFIX) -ldl
