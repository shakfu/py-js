run the python file `build.py` with your system python3

for example:

     python3 build.py


This will do two things:

1. configure the xcodeproject by writing to the `py-js.xcconfig` file
2. run xcodebuild to generate the externals



